---
import Base from "../layouts/Base.astro";
import Home from "../components/Home.astro";
import { getSiteData } from "../lib/github";

const data = await getSiteData();
---

<Base active="home" stars={data.stars}>
  <Home />
</Base>
